# The address that receives notes is saved in the app.
# These settings are the mailbox that sends them. Restart after changing this file.
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
SMTP_FROM=
# Optional. The link in each note. Defaults to this app's address.
APP_URL=
